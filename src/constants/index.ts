/**
 * Application constants
 */

// Route names
export const ROUTE_NAMES = {
  HOME: 'home',
  ABOUT: 'about',
  LOGIN: 'login',
} as const

// API response codes
export const API_CODE = {
  SUCCESS: 20000,
  SUCCESS_ALT: 200,
  UNAUTHORIZED: 401,
} as const

// Storage keys
export const STORAGE_KEY = {
  TOKEN: 'token',
} as const
